<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:theme="@style/debugging_theme">

    <!--Aquí carga los fragmentos ¿ -->
    <FrameLayout
        android:id="@+id/frame_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/bottom_navigation"
        android:layout_weight="1" />

    <!--La barrita con los botones -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@color/Secondary"
        android:color="@color/Primary"
        app:itemIconTint="@color/color_selector"
        app:itemTextColor="@color/onPrimary"
        app:itemActiveIndicatorStyle="@style/debugging_theme.NavBar"
        app:menu="@menu/bottom_navigation" /> <!-- Referencia al menú -->

</RelativeLayout>
